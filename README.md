# HTML-and-CSS-Template-tow
A fully responsive multi-section template built using pure HTML5 and CSS3. Implemented using Flexbox, CSS Grid, media queries, and reusable components. This is a clean static template (no dynamic functionality), designed for layout practice and UI structure
